# === Required: Authentication Credentials ===
# Change these to your own secure values!
SOCKS_USER=my_secure_username
SOCKS_PASS=ChangeMe_To_A_VeryStrong_Password_AtLeast24Chars!

# === Port Configuration ===
# Use 443 for Cloudflare Workers compatibility
SOCKS_PORT=443

# Health check HTTP port (Railway assigns this automatically via $PORT)
HEALTH_PORT=8080

# === Rate Limiting ===
# Max connections per IP within the time window
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX=50

# === Connection Settings ===
# Timeout in seconds for connecting to destination
CONNECT_TIMEOUT=15